#include "main.h"

/**
 * puts2 - prints part of a string
 * @str: string to be printed
 *
 * Return: void
 **/
void puts2(char *str)
{
int y = 0;
while (str[y] != '\0')
{
if (y % 2 == 0)
_putchar(str[y]);
i++;
}

_putchar('\n');
}

