#include<stdio.h>
/**
 *main - Entry point
 *Return: Always 0 (success)
 */

int main(void)
{
	char a;
	int b;
	float c;
	
printf("size of char: %lu bytes(s)\n", sizeof(a));
printf("size of char: %lu bytes(s)\n", sizeof(b));
printf("size of char: %lu bytes(s)\n", sizeof(c));
return (0);

}
